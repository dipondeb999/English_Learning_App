import 'package:flutter/material.dart';

class GovtExamScreen extends StatelessWidget {
  const GovtExamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Govt Exam Screen'),
      ),
    );
  }
}